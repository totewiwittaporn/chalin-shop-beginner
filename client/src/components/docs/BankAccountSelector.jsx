export function BankAccountSelector({ scope, ownerId, value, onChange }) {
  // โหลด list ด้วย bankAPI.listBranch / listPartner ตาม scope
  // แสดง default account เป็นตัวเลือกแรก (ถ้ามี isDefaultForReceipt / isDefaultForBill ตามชนิดเอกสาร)
}
