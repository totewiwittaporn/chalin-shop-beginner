// prisma/schema/enums.prisma

enum Role {
  ADMIN
  STAFF
  CONSIGNMENT
  QUOTE_VIEWER
}

enum DocKind {
  QUOTE
  INVOICE
  RECEIPT
  DELIVERY
  CONSALE
}

enum DocStatus {
  DRAFT
  ISSUED
  PARTIAL
  PAID
  SHIPPED
  CANCELLED
}

enum PartnerStatus {
  ACTIVE
  INACTIVE
}

enum StockLedgerType {
  PURCHASE_RECEIVE // รับเข้าจากใบสั่งซื้อเข้าคลังสาขา
  ADJUST_IN // ปรับเพิ่ม
  ADJUST_OUT // ปรับลด
  TRANSFER_IN // โอนย้ายเข้า (ระหว่างสาขา)
  TRANSFER_OUT // โอนย้ายออก (ระหว่างสาขา)
  SALE // ตัดสต็อกจากการขายหน้าร้าน
  CONSIGN_SEND // ส่งสินค้าไปที่ร้านฝากขาย (สต็อกสาขา - , สต็อกฝากขาย +)
  CONSIGN_RETURN // ร้านฝากขายตีกลับ/คืน (สต็อกฝากขาย - , สต็อกสาขา +)
  CONSIGN_SALE // ร้านฝากขายขายออก (สต็อกฝากขาย -)
}

enum StockCountScope {
  BRANCH
  CONSIGNMENT
}

enum StockCountStatus {
  OPEN
  FINALIZED
}

enum TransferStatus {
  DRAFT
  SENT
  RECEIVED
  CANCELLED
}

enum PaymentMethod {
  CASH
  TRANSFER
  CARD
}

enum PaymentKind {
  CASH
  TRANSFER
  CARD
}

enum DocType {
  BILL_CONSIGNMENT
  RECEIPT_STANDARD
  DELIVERY_BRANCH
  DELIVERY_CONSIGNMENT
}

enum PartyKind {
  HEADQUARTERS
  BRANCH
  CONSIGNMENT
  CUSTOMER
}

enum ConsignDeliveryDocTemplate {
  CAT_ONLY       // ใช้หมวดร้านฝากขาย
  CODE_AND_CAT   // ใช้รหัสและหมวดร้านฝากขาย
}
